{
  "version": 2,
  "buildCommand": "npm ci || (rm -rf node_modules && npm install) && npm run build",
  "outputDirectory": "build",
  "installCommand": "npm ci || npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

